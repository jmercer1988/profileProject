import { JmFormatDatePipe } from './jm-format-date.pipe';

describe('JmFormatDatePipe', () => {
  it('create an instance', () => {
    const pipe = new JmFormatDatePipe();
    expect(pipe).toBeTruthy();
  });
});
